project: dp_benchmark
method: bayes
metric:
  goal: minimize
  name: val_loss
parameters:
  config: 
    distribution: categorical 
    values: 
      - configs.yaml
  # Simple ConvNet params, see models.py
  model.interm_n_channels: 
    distribution: int_uniform
    max: 16
    min: 2
  model.end_n_channels:
    distribution: int_uniform
    max: 32
    min: 4
  model.kernel_size: 
    distribution: int_uniform
    max: 8
    min: 2
  # DP params
  model.L2_clip:
    distribution: uniform
    max: 20
    min: 0.5
  model.batch_size:
    distribution: int_uniform
    max: 32
    min: 8
  model.noise_multiplier:
    distribution: uniform
    max: 5
    min: 0.05
  # NOTE: uncomment if this should be considered
  # trainer.max_epochs:
  #   distribution: int_uniform
  #   max: 100
  #   min: 10
  # model.target_delta:
  #   distribution: uniform
  #   max: 2e-05
  #   min: 5e-06
  # model.target_epsilon:
  #   distribution: uniform
  #   max: 100
  #   min: 10
program: trainer.py